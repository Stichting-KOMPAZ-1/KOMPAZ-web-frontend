# Prepended by the API client to the paths in the generated SDK, which already
# start with /api. Exists for deployments where the API is not
# served from the same origin as the app.
VITE_API_BASEURL=""

# Backend the dev server proxies /api to.
VITE_API_PROXY_TARGET=""
