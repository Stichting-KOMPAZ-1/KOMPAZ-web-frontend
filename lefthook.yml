pre-commit:
  parallel: true
  jobs:
    - run: bun oxlint {staged_files}
      glob: "*.{js,jsx,ts,tsx}"
    - run: bun oxfmt --check {staged_files}
      glob: "*.{js,jsx,mjs,cjs,ts,tsx,mts,cts,css,scss,json,jsonc,html}"
